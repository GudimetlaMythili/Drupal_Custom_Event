# DreamWorks Event Planner - Environment Configuration

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_DB=drupal
POSTGRES_USER=drupal
POSTGRES_PASSWORD=drupal

# ===========================================
# Drupal Configuration
# ===========================================
DRUPAL_ADMIN_USER=admin
DRUPAL_ADMIN_PASS=admin
DRUPAL_HASH_SALT=event-planner-salt-change-in-production-abc123

# ===========================================
# Email Configuration
# ===========================================
# Set MAIL_MODE to choose email provider:
#   - mailhog    : Use MailHog for testing (default)
#   - gmail      : Use Gmail SMTP
#   - outlook    : Use Outlook/Office 365 SMTP
#   - sendgrid   : Use SendGrid
#   - custom     : Use custom SMTP settings

MAIL_MODE=mailhog

# Gmail Configuration (if MAIL_MODE=gmail)
# Note: Use App Password, not your regular password
# Create at: https://myaccount.google.com/apppasswords
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# Outlook/Office 365 Configuration (if MAIL_MODE=outlook)
OUTLOOK_USER=your-email@outlook.com
OUTLOOK_PASSWORD=your-password

# SendGrid Configuration (if MAIL_MODE=sendgrid)
SENDGRID_API_KEY=your-sendgrid-api-key

# Custom SMTP Configuration (if MAIL_MODE=custom)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-username
SMTP_PASSWORD=your-password
SMTP_FROM=noreply@example.com
SMTP_TLS=on

# ===========================================
# Container Ports
# ===========================================
# Drupal: http://localhost:8080
# MailHog UI: http://localhost:8025 (only in mailhog mode)
